// 
const initialState = []

export default (state = initialState, action) => {
    switch (action.type) {
        case 'FETCH_ALL':

            break;
        case 'CREATE_ALL':

            break;

        default:
            return state;
    }
}
